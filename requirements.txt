# === Core Components ===

# Kafka Client (ที่คุณมี)
kafka-python==2.0.2

# Spark (แนะนำ 3.4.1 เพื่อความเข้ากันได้กับ ES Connector)
pyspark==3.4.1

# PostgreSQL Client (ที่คุณมี)
psycopg2-binary==2.9.9

# Elasticsearch Client (สำหรับ Task 1 ใน Airflow)
elasticsearch>=8.0.0,<9.0.0

# === Airflow ===

# Airflow Core (ใช้เวอร์ชันเดียวกับใน Docker Compose หรือที่คุณติดตั้ง)
# apache-airflow==2.7.3

# Airflow Providers (ส่วนเสริมสำหรับเชื่อมต่อระบบอื่น)
apache-airflow-providers-postgres # จำเป็นสำหรับ Airflow Backend และ Task 3
apache-airflow-providers-elasticsearch # จำเป็นสำหรับ Task 1 (ถ้าใช้ Hook/Operator)
apache-airflow-providers-apache-spark # ถ้าจะใช้ SparkSubmitOperator ใน Task 2
apache-airflow-providers-docker # ถ้าจะใช้ DockerOperator ใน Task 2

# === Data Processing & Utilities ===

# Data Processing (ที่คุณมี)
pandas==2.1.3
numpy==1.26.2

# Database ORM/Toolkit (ใช้งานกับ psycopg2 ได้ง่ายขึ้น)
SQLAlchemy>=1.4,<2.0

# Utilities (ที่คุณมี)
python-dateutil==2.8.2

# --- Optional: API (ถ้าจะทำ Phase 3) ---
# fastapi
# uvicorn[standard]
# pydantic

# --- Optional: Development Tools ---
# black         # Code formatter
# flake8        # Linter
# pytest        # Testing framework